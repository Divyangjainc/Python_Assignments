//Convert days into months
# Accept number of days from user
days = int(input("Enter number of days: "))

# Convert to months (approximate)
months = days / 30

# Output result
print("Equivalent months (approx):", round(months, 2))
